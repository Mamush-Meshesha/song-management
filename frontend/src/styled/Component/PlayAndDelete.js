import styled from "@emotion/styled"
const PlayAndDelete = styled.div`
    hieght: 80px;
    width: 100%;
    border-bottom: 1px;
    display: flex;
    align-items: center;
    position: absolute;
    background-color: black;
    opacity: 0.7
    padding: 0 24px

`
const Inner = styled.div`
    display: flex;
    justify-content: space-between;
    width: full
`
export {PlayAndDelete, Inner}